================================================================================
  MTGBALALA — TODO LIST & IMPLEMENTATION NOTES
  Generated: 2026-03-07
================================================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [SECTION 1]  EXISTING TODOs (found in codebase)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FILE: CardDatabase.h
  Line 24 │ loadAllCards()
  → TODO: Json reader, sau 30 de linii ce cpp cu fiecare carte
    Cards are currently hardcoded one-by-one. Either parse a JSON/text file
    or define all cards in a separate .cpp to keep the database clean.

  Line 38 │ getRandomCard()
  → TODO: TS (to be implemented)
    Returns nullptr. Needs to pick a random card from `library` using
    a uniform distribution. Required by the Shop and Draft systems.

FILE: Effect.h
  Line 32 │ AddManaEffect::resolve()
  → TODO: mana system
    state.addMana() is commented out because ManaPool / mana tracking is
    not wired into RoundTracker yet. Implement a ManaPool inside
    RoundTracker (red, blue, green counters) and hook this effect up.

FILE: PlayerInfo.h
  Line 7  │ File top
  → TODO: Forward declarations — remove #include placeholders later
    `class Card;` and `class Relic;` are stubs. Once Card.h and Relic.h
    are finalised, replace them with proper includes.

  Line 17 │ PlayerInfo class body
  → TODO: add deck and relics when Card.h / Relic.h are ready
    The commented-out member vectors (m_deck, m_relics) need to be
    uncommented and integrated once both classes are stable.

FILE: RoundTracker.h
  Line 22 │ drawCard()
  → TODO: maybe lose? either way add an std exception later
    When the deck runs empty, currently just prints a message. Decide:
      (a) trigger a run-loss condition, or
      (b) auto-reshuffle graveyard into deck (like Balatro).
    Either way throw a proper exception or call GameManager to change state.

FILE: view.h
  Line 38 │ showDraft()
  → TODO: populate ZoneManager::deck with chosen cards
    Draft screen is a stub that skips straight to COMBAT. Needs to call
    CardDatabase, present 10 random cards, let the player pick 5, then
    push those into the active deck.

  Line 58 │ showCombat()
  → TODO: ZoneManager draws card, EventQueue fires "CardPlayed",
          PassiveManager resolves triggers, RunTracker updates score.
    Playing a card is currently just an inline score bump. Full pipeline:
      1. ZoneManager pops card from hand
      2. card.play(roundTracker) dispatches effects
      3. EventQueue broadcasts CARD_PLAYED
      4. PassiveManager iterates relics/enchantments for triggered effects
      5. RoundTracker accumulates score


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [SECTION 2]  ADDITIONAL THINGS TO IMPLEMENT
  (based on specs.txt, current architecture, and game design)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

──────────────────────────────────────────────────────────────────────────────
  A. CORE ENGINE
──────────────────────────────────────────────────────────────────────────────

[ ] ManaPool class  (Effect.h references it; GameManager.h declares m_mana)
    Members : int red, blue, green
    Methods : addMana(r,b,g), spendMana(r,b,g) -> bool, reset(), printStatus()
    The View and Combat loop already call mana.reset() and mana.printStatus() —
    the class just needs to exist and be properly #included.

[ ] GameState enum  (view.h and GameManager.h reference it but it is never defined)
    enum class GameState { MAIN_MENU, DRAFT, COMBAT, SHOP, GAME_OVER };
    Without this enum the project does not compile. Define it in its own header
    (e.g. GameState.h) and include it where needed.

[ ] Relic.h  (PlayerInfo.h and the specs both mention relics)
    Passive items that sit on the board and react to events.
    Should implement IEffect or a similar interface so they can be registered
    with the EventQueue. Examples: "gain 1 mana for every card played",
    "draw an extra card at the start of each round".

[ ] Score Formula  (RunTracker.h line 15 — marked as "mega provizorie")
    Current: 100 + (round-1)*50  (linear)
    Consider an exponential or step-based curve matching Balatro's blinds.
    Formula should live in RunTracker::targetScore() and be easy to tweak.

──────────────────────────────────────────────────────────────────────────────
  B. CARD SYSTEM
──────────────────────────────────────────────────────────────────────────────

[ ] ScoreEffect  (a third IEffect subclass alongside DrawCardEffect/AddManaEffect)
    Needed for "Payload" cards that add points based on Storm Count.
    resolve() should call state.addScore(baseValue + stormBonus * state.stormCount).

[ ] Card type hierarchy  (currently all cards are flat Card objects)
    Consider subclasses:
      - RitualCard    : plays AddManaEffect
      - CantripCard   : plays DrawCardEffect then possibly a ScoreEffect
      - PayloadCard   : ScoreEffect scaled by storm count
    Alternatively, keep the flat Card + effect-list model (current approach)
    and just ensure the CardDatabase wires up the right IEffect objects.

[ ] modifier struct  (declared in Card.h line 8 but never defined)
    The `modifier` struct is forward-declared and stored as shared_ptr.
    Define it: what fields does it have? (e.g. cost reduction, damage bonus)
    Until this is defined the Card copy-constructor silently does nothing
    with mods.

[ ] Card JSON / data file  (CardDatabase.h TODO line 24)
    Suggested format — cards.json:
      [
        { "id": "c_brainstorm", "name": "Brainstorm", "red": 1, "rarity": "C",
          "effects": [{"type": "draw", "amount": 3}] },
        ...
      ]
    Use a lightweight JSON lib (e.g. nlohmann/json, header-only) or write a
    simple hand-rolled parser if external deps are not allowed.

[ ] CardDatabase::getRandomCard()  (TODO TS, line 38)
    Implementation:
      auto it = library.begin();
      std::advance(it, rand() % library.size());
      return createCard(it->first);
    Use mt19937 instead of rand() for proper distribution.

──────────────────────────────────────────────────────────────────────────────
  C. ZONE MANAGEMENT
──────────────────────────────────────────────────────────────────────────────

[ ] CardZone::empty() naming collision
    std::vector::empty() returns bool; your empty() returns void and clears.
    Rename to clear() or clearAll() to avoid confusion.

[ ] Exile zone logic
    CardZone exile already exists in RoundTracker. Missing: when does a card
    get exiled vs go to graveyard? Define the rule and implement the routing
    in view.h / card play logic.

[ ] Deck initialisation from Draft
    After draft phase the player's chosen cards must be pushed into the
    RoundTracker::deck before combat starts. The call site is view.h showDraft().
    Requires access to CardDatabase::createCard() for each chosen ID.

[ ] Graveyard → Deck reshuffle  (for empty-deck edge case in RoundTracker)
    If the player runs out of cards, move all graveyard cards back to deck
    and shuffle. This mirrors Balatro's "deck recycle" behaviour.

──────────────────────────────────────────────────────────────────────────────
  D. EVENT SYSTEM  (Observer pattern — specs.txt Phase 1)
──────────────────────────────────────────────────────────────────────────────

[ ] IEventListener interface
    virtual void onEvent(EventType type, RoundTracker& state) = 0;

[ ] EventType enum
    enum class EventType { CARD_PLAYED, MANA_GENERATED, CARD_DRAWN, ROUND_END };

[ ] EventQueue / EventBus class
    - Holds a list of IEventListener* (relics, enchantments)
    - Provides fire(EventType) which iterates listeners
    - RoundTracker or GameManager owns the EventQueue
    - card.play() fires CARD_PLAYED; drawCard() fires CARD_DRAWN; etc.

[ ] PassiveManager / Relic triggers hooked to EventQueue
    Each Relic registers itself as an IEventListener at round start.
    When CARD_PLAYED fires, relics that care about it execute their bonus.

──────────────────────────────────────────────────────────────────────────────
  E. META-LOOP (Shop & Draft)
──────────────────────────────────────────────────────────────────────────────

[ ] Draft Phase (showDraft stub in view.h)
    Step 1: CardDatabase::getRandomCard() × 10 to build the offer pool.
    Step 2: Display all 10, player picks 5.
    Step 3: Push chosen cards via CardDatabase::createCard() into the deck.
    Step 4: Shuffle the deck before transitioning to COMBAT.

[ ] Shop phase (showShop stub in view.h)
    - Display 3 random cards (weighted by rarity) + 1 booster pack.
    - Use PlayerInfo::spendGold() to handle purchases.
    - Allow paying to trash/remove a card from the master deck (see specs.txt).
    - Advance run round via RunTracker::advanceRound() after shop closes.

[ ] Economy / Gold rewards  (ActiveRun::triggerPostRoundRewards exists but is basic)
    Current formula: targetScore / 100 gold per round.
    Consider: base gold + bonus for cards remaining in hand, rounds won streak,
    or rarity of cards played — mirrors Balatro's chip bonus system.

[ ] Booster pack logic
    A booster pack reveals 3 cards; the player keeps 1. Needs CardDatabase
    to pull rarity-weighted random cards. Rare/Legendary should be rarer.
    Rarity weights suggestion: C=60%, U=25%, R=13%, L=2%.

──────────────────────────────────────────────────────────────────────────────
  F. ARCHITECTURE & CODE QUALITY
──────────────────────────────────────────────────────────────────────────────

[ ] Circular include / forward declaration cleanup  (PlayerInfo.h TODO line 7)
    Once Card.h is stable, remove the `class Card;` stub from PlayerInfo.h
    and add the proper #include. Same for Relic.h when it is created.

[ ] RoundTracker::startNewRound() hardcodes discardsLeft = 3
    Should pull from PlayerInfo::getMaxDiscards() via ActiveRun, not a magic
    number. Change to: discardsLeft = currentRun.player.getMaxDiscards();

[ ] view.h references undefined members (run.currentTurn, run.stormCount, etc.)
    RunTracker has no public currentTurn or currentScore fields — they are in
    RoundTracker. The view.h was written against the old merged tracker.
    Decide: either expose them via getters, or split responsibilities clearly
    between RunTracker (meta run) and RoundTracker (per-round state).

[ ] Seed Deck::shuffle() with a proper RNG
    Current: std::default_random_engine{} — seeded to 0 every time.
    Fix: std::default_random_engine{ std::random_device{}() };

[ ] Unit tests
    Add a simple test runner (or use catch2 / doctest header-only).
    Priority tests:
      - Deck::shuffle() produces different orders across multiple calls
      - CardZone::moveCardTo() does not duplicate cards
      - RoundTracker::isRoundWon() returns true when score >= target
      - AddManaEffect / DrawCardEffect resolve correctly


================================================================================
  END OF FILE
================================================================================
